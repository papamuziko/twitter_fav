server {
    listen      80;
    server_name localhost;
    charset     utf-8;
    client_max_body_size 75M;

    location / { try_files $uri @twitter_fav; }
    location @twitter_fav {
        include uwsgi_params;
        uwsgi_pass unix:/home/ubuntu/twitter_fav/app_uwsgi.sock;
    }
}